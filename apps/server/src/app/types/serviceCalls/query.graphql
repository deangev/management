type Query {
    serviceCallsData(estateID: String): ServiceCallsResponse!
    serviceCallData(serviceCallID: String!): ServiceCall!
}

type ServiceCallsResponse {
    serviceCalls: [ServiceCall!]
    hits: Int
}