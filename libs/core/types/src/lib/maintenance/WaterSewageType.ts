export interface WaterType {
  corporation: string;
  main_counter_location: string;
  main_counter_number: number;
  private_counters_location: string;
  note: string;
}

export interface SewageType {
  note: string;
}
